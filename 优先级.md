# 途畔归所 · 开发优先级（2026-05-09 最终修订版）
> 基于 [2026-05-09] 网络架构调试进展：客户端本地Player生成与摄像机冲突修复阶段  
> **核心原则**：网络优先，单机功能全面冻结；暂停所有非网络相关开发，集中资源打通完整联机同步管道  
> **关键变更**：
> - 🔴 建筑系统整体移除，所有相关内容无限期推迟
> - 🟢 阶段D1网络基础层已全部完成并通过基础测试
> - 🟠 阶段D2网络场景管理进入联调阶段
> - ⚪ 所有单机功能（采集、装备、UI美化）全部暂缓，仅保留必要的bug修复
> - 架构映射：`ItemManager`(ObjectDB)、`NetObjManager`(ZNetScene)、`NetCore`(ZNet+ZDOMan合并)

---

## 🔴 阶段 D：联机同步核心（当前唯一最高优先级）
**所有开发资源集中于此，非紧急bug一律延后处理**

### D1：网络基础与分布式对象层 ✅（全部完成）
- ✅ LAN广播发现：UDP房间信息广播与监听
- ✅ ENet连接：主机创建/客户端加入、`PeerConnected`/`PeerDisconnected`事件
- ✅ `NetCore`升级：合并连接管理+RPC路由+分布式对象管理
- ✅ `NetID`结构体：`UserID`+`ID`构成唯一网络标识
- ✅ `NetObj`数据容器：`Position`/`Rotation`/`PrefabHash`/`OwnerPeerID`/修订号/`Vars`
- ✅ `Spawn`/`Destroy`流程：主机创建`NetObj`广播`ObjCreate`/`ObjDestroy`
- ✅ 同步调度：20Hz定时收集脏数据，按Peer增量发送`ObjSync`
- ✅ 自定义RPC收发：`SendRpcToPeer`/`BroadcastRpc`/`ReceiveCustomPackets`
- ✅ `NetSyncBase`组件设计：绑定`NetObj`、所有权判断、对象RPC注册
- ✅ `NetTransformSync`组件设计：Owner写入/非Owner插值平滑跟随
- ✅ 单机/主机`IsHost`判断逻辑修复
- ✅ 预制体哈希不匹配问题修复（统一使用`CatUtils.GetStableHashCode`）
- ✅ `NetObjectManager.Instance`空引用问题修复
- ✅ `HandleSpawned`中`GlobalPosition`访问报错修复
- ✅ `Player.cs`中`m_PlayerData`为空问题修复
- ✅ 主机端成功生成Player节点，`HandleSpawned`流程正常

### D2：网络场景管理 🔄（进行中，当前焦点）
- ✅ `NetObjManager`核心实现：预制体注册表、监听`Spawn`/`Destroy`自动创建/销毁节点
- ✅ 网络对象预制体注册：Player预制体已注册到`NetObjManager.m_PrefabDict`
- ✅ 主机端`SpawnLocalPlayer`流程正常，可成功生成本地Player
- 🔄 `PlayerManager`重构：已实现临时客户端本地生成方案，待切换为网络事件驱动
- 🔲 客户端`ObjCreate`RPC接收与处理调试（当前客户端未走正式网络生成管道）
- 🔲 验证`NetObjectManager`在客户端的初始化与事件订阅状态

### D3：玩家与实体同步 🔄（进行中）
- ✅ `Player._Ready`摄像机激活逻辑优化：仅本地玩家相机激活
- ✅ `PlayerController`所有权检查修复：非Owner玩家阻断输入
- 🔄 `NetTransformSync`组件集成测试（已挂载至Player预制体，待联调）
- 🔲 双客户端验证：主机移动 → 客户端可见并平滑跟随
- 🔲 玩家动画同步（`NetAnimationSync`组件实现）
- 🔲 NPC网络生成与位置同步

### D4：交互网络化 🔲（待启动）
- 🔲 容器操作网络化：打开/关闭、物品转移封装为RPC
- 🔲 物品拾取/丢弃同步：服务器生成掉落物`NetObj`，客户端请求拾取
- 🔲 攻击与伤害同步：攻击发起RPC，服务器计算伤害并广播结果

---

## 🟢 已完成（历史功能，暂不修改）

### 阶段 A1：物品拾取与放置物基础
- ✅ 玩家射线拾取（`m_eye`驱动，移除旧检测区域）
- ✅ `IInteractable`交互接口统一（E/F键）
- ✅ `PlacedBase`继承体系建成，统一类型枚举（建筑/容器/工作台）
- ✅ `PlacedManager`资源加载与实例化
- ✅ `PlacedData`资源定义（ID、类型、图标）
- ✅ 物品"世界掉落 ↔ 背包"拾取/丢弃闭环

### 阶段 A2：容器核心逻辑
- ✅ `ContainerComp`继承`PlacedBase`，作为可交互放置物
- ✅ 实现`IInventoryHolder`（内存字典临时持有物品）
- ✅ 按E打开/关闭容器UI（`m_IsOpen`状态管理）
- ✅ 容器/背包跨库存拖拽转移（`SwapSlots`+`TransferTo`深度拷贝）
- ✅ 容器UI基础：复用`InventoryComp`双面板

### 阶段 A3：容器 UI 完善
- ✅ 跨库存拖拽（同库存交换、跨库存转移）
- ✅ 按E切换容器开关
- 🔲 双面板美化（玩家背包/容器明确视觉区分）—— 移交美术，暂缓
- 🔲 物品过滤器接口（仅展示/接收特定类型）—— 预留字段，暂缓

### 阶段 A4：通用持久化层（类 Valheim ZDO）
- ✅ `PlacedBase`添加`m_PlacedGUID`（唯一标识）
- ✅ `PlacedBase`添加`CustomData`动态属性字典
- ✅ `ContainerComp.LoadInventory`/`SaveInventory`读写`CustomData["container_items"]`
- ✅ `SaveData`增加`PlacedRuntimeData`字典
- ✅ 容器关闭时写入持久化，场景加载时恢复
- ✅ **验证通过**：放物品 → 关闭箱子 → 保存 → 重启 → 物品完整保留

### 阶段 A5：Player UI 分离
- ✅ 新建`PlayerUIHandler`类，统一管理背包/控制台/暂停菜单
- ✅ `Player`只保留交互路由，不再直接持有或操作UI节点
- ✅ 鼠标捕获/释放、面板互斥逻辑移至`PlayerUIHandler`
- ✅ 现有容器开闭、控制台切换等行为不变，回归测试通过

### 阶段 A6：实体基类提取
- ✅ 提取`EntityBase (StaticBody3D)`，统一`m_EntityGUID`与`CustomData`
- ✅ `PlacedBase`重构为继承`EntityBase`，移除重复字段
- ✅ `CreatureBase`保持独立体系，形成Entity ↔ Creature清晰边界
- ✅ `ResourceBase`继承`EntityBase`，作为所有可采集资源基类
- ✅ `TreeComp`继承`VegetationComp`完成封装，就绪可交互

### 阶段 C：世界资源采集（核心攻击闭环已完成，其余全面暂缓）
- ✅ 按键映射`cat_Attack`，用于攻击/采集
- ✅ `Player`实现`PerformAttack`，通过射线命中目标调用`IDamageable.TakeDamage`
- ✅ `IDamageable`接口实现，`VegetationComp`每受攻击削减耐久
- ✅ `StateMachine`挂载为Player子节点，管理Idle/Walk/Attack等状态
- ✅ 攻击动画使用`AnimationNodeOneShot`，关键帧触发伤害
- ✅ 攻击期间锁定移动，动画结束切回Idle
- ✅ **测试成功**：攻击动画播放、关键帧伤害、树木扣血均正常
- 🔲 资源节点数据完善（掉落表、重生时间等）—— 全面暂缓
- 🔲 资源耗尽后的视觉/物理反应 —— 全面暂缓
- 🔲 掉落物品生成与自动拾取 —— 全面暂缓
- 🔲 采集视觉/音效反馈预留接口 —— 全面暂缓

### 阶段 G：物品系统扩展（装备基础测试通过，其余全面暂缓）
- ✅ `ItemData`扩展武器属性（攻击力、类型、攻击范围）
- ✅ 武器装备流程：`SlotComp` → `InventoryComp` → `Player.EquipWeapon`
- ✅ `ItemComp`装备状态下冻结物理、关闭碰撞、变为纯视觉Mesh
- 🔲 武器Hitbox动态启用与伤害判定 —— 全面暂缓
- 🔲 `SlotComp`右键弹出上下文菜单 —— 全面暂缓
- 🔲 快捷键栏支持 —— 全面暂缓
- 🔲 工具等级影响采集效率 —— 全面暂缓

---

## 🟠 阶段 B：HUD 与玩家状态（全面暂缓）
> 代码侧已搭建框架，美术资源制作中，全部推迟至网络完成后

- 🔲 常驻HUD：生命值、体力值、快捷栏 → UI场景搭建已移交美术，暂缓
- 🔲 看向交互目标时显示目标名称与交互提示 —— 暂缓
- 🔲 `EscComp`逻辑补全：保存并退出、返回主菜单 —— 暂缓
- 🔲 `PlayerData`已添加生命/体力字段，`Player`已添加伤害/治疗/体力消耗接口

---

## 🟣 阶段 F：实体属性与 AI 深化（远期，全面暂缓）
- 🔲 属性系统正式设计（生命/耐力/力量/负重）
- 🔲 NPC状态机扩展（战斗、死亡、逃跑）
- 🔲 NPC尸体作为可搜索容器（掉落）
- 🔲 受伤反馈、击退

---

## ⚪ 远期规划
- 任务/剧情系统
- 动态难度与生态
- 完整联机房间管理
- 资源热加载与工具链
- 植被群落自动生成/刷新

---

## 🎯 当前紧急行动清单（按优先级排序）
1. 验证临时方案：确认`SpawnLocalClientPlayer`已正确实现，客户端可正常生成Player并移动
2. 修复摄像机冲突：按优化后的`Player._Ready`逻辑部署，确保联机时只有本地玩家相机激活
3. 验证所有权隔离：测试主机端多个Player节点，确认只有本地玩家响应输入
4. 排查客户端网络生成管道：
   - 移除客户端临时保底代码
   - 检查`NetCore`在客户端是否正确接收`ObjCreate`RPC
   - 验证`NetObjectManager.OnSpawned`事件在客户端是否触发
5. 集成`NetTransformSync`：完成位置/旋转同步与插值联调
6. 双客户端验证：主机创建玩家 → 客户端可见 → 主机移动 → 客户端平滑跟随