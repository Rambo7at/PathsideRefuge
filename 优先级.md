# 途畔归所 · 开发优先级（修订版）
> 基于 [2026-05-06] 容器持久化闭环完成，Player UI 分离完成，装备测试通过  
> 核心原则：先单机闭环，后联机同步；当前已完成基础交互闭环，**即刻转入网络底层搭建**  
> 关键变更：通用持久化层验证通过；Player UI 分离完成；EntityBase 体系确立；阶段 C 攻击框架与砍树闭环打通；阶段 G 装备测试成功。**为规避后期重构风险，联机同步优先级提至最高。**

---

## 🟢 已完成

### 阶段 A1：物品拾取与放置物基础
- ✅ 玩家射线拾取（`m_eye` 驱动，移除旧检测区域）
- ✅ `IInteractable` 交互接口统一（E/F 键）
- ✅ `PlacedBase` 继承体系建成（原 `PlacedComp`），统一类型枚举（建筑/容器/工作台）
- ✅ `PlacedManager` 资源加载与实例化
- ✅ `PlacedData` 资源定义（ID、类型、图标）
- ✅ 物品“世界掉落 ↔ 背包”拾取/丢弃闭环（`InventoryComp` 跨库存方法已备）

### 阶段 A2：容器核心逻辑
- ✅ `ContainerComp` 继承 `PlacedBase`，作为可交互放置物
- ✅ 实现 `IInventoryHolder`（内存字典临时持有物品）
- ✅ 按 E 打开/关闭容器 UI（`m_IsOpen` 状态管理）
- ✅ 容器/背包跨库存拖拽转移（`SwapSlots` + `TransferTo` 深度拷贝，两侧库存同步刷新与存档）
- ✅ 容器 UI 基础：复用 `InventoryComp` 双面板，可同时显示玩家背包与容器库存

### 阶段 A3：容器 UI 完善
- ✅ 跨库存拖拽（同库存交换、跨库存转移）
- ✅ 按 E 切换容器开关
- 🔲 双面板美化（玩家背包/容器明确视觉区分）—— 移交美术
- 🔲 物品过滤器接口（仅展示/接收特定类型）—— 预留字段

### 阶段 A4：通用持久化层（类 Valheim ZDO）
- ✅ `PlacedBase` 添加 `m_PlacedGUID`（唯一标识，手工指定或自动生成）
- ✅ `PlacedBase` 添加 `CustomData` 动态属性字典（`Dictionary<string, Variant>`）
- ✅ `ContainerComp.LoadInventory` / `SaveInventory` 读写 `CustomData["container_items"]`
- ✅ `SaveData` 增加 `PlacedRuntimeData` 字典（键为 `m_PlacedGUID`）
- ✅ 容器关闭时写入 `PlacedRuntimeData`，通过 `SaveManager.SaveData()` 持久化
- ✅ 场景/容器 `_Ready` 时从 `PlacedRuntimeData` 恢复 `CustomData`，重建物品
- ✅ **验证通过**：放物品 → 关闭箱子 → ESC 保存 → 重启游戏 → 箱子物品完整保留

### 阶段 A5：Player UI 分离
- ✅ 新建 `PlayerUIHandler` 类，统一管理背包/控制台/暂停菜单
- ✅ `Player` 只保留交互路由（`CheckRaycastInteract`），不再直接持有或操作 UI 节点
- ✅ 鼠标捕获/释放、面板互斥逻辑移至 `PlayerUIHandler`
- ✅ 现有容器开闭、控制台切换等行为不变，回归测试通过

### 阶段 A6：实体基类提取
- ✅ 提取 `EntityBase (StaticBody3D)`，统一 `m_EntityGUID` 与 `CustomData`
- ✅ `PlacedBase` 重构为继承 `EntityBase`，移除重复字段
- ✅ `CreatureBase` 保持独立体系，形成 Entity ↔ Creature 清晰边界
- ✅ `ResourceBase` 继承 `EntityBase`，作为所有可采集资源基类
- ✅ `TreeComp` 继承 `VegetationComp` 完成封装，就绪可交互

---

## 🟠 阶段 B：HUD 与玩家状态（部分移交美术）
> **代码侧已搭建框架，美术资源制作中**

- 🔲 常驻 HUD：生命值、体力值、快捷栏 → **UI 场景搭建已移交美术**
- 🔲 看向交互目标时显示目标名称与交互提示（交互系统已预留接口）
- 🔲 `EscComp` 逻辑补全：保存并退出、返回主菜单
- 🔲 `PlayerData` 已添加生命/体力字段，`Player` 已添加伤害/治疗/体力消耗接口

---

## 🟡 阶段 C：世界资源采集（核心攻击闭环已完成，其余暂缓）
**当前状态**：玩家攻击框架、砍树耐久、动画控制已闭环并通过测试。  
**剩余内容推迟至联机同步完成后再继续。**

- ✅ 按键映射 `cat_Attack`，用于攻击/采集
- ✅ `Player` 实现 `PerformAttack`，通过射线命中目标调用 `IDamageable.TakeDamage`
- ✅ `IDamageable` 接口实现，`VegetationComp` 每受攻击削减耐久
- ✅ `StateMachine` 挂载为 Player 子节点，管理 Idle/Walk/Attack 等状态
- ✅ 攻击动画使用 `AnimationNodeOneShot`，关键帧方法调用轨道触发 `PerformAttack`
- ✅ 攻击期间锁定移动，动画结束由 `EndAttack` 切回 Idle
- ✅ **测试成功**：攻击动画播放、关键帧伤害、树木扣血均正常

- 🔲 资源节点数据完善（`ResourceNodeData` 掉落表、重生时间等）—— **暂缓**
- 🔲 资源耗尽后的视觉/物理反应（模型切换、粒子/音效）—— **暂缓**
- 🔲 掉落物品生成与自动拾取 —— **暂缓**
- 🔲 采集视觉/音效反馈预留接口 —— **暂缓**

---

## 🟢 阶段 G：物品系统扩展（装备基础测试通过，其余暂缓）
**当前状态**：`CheckBox` 装备调用链跑通，武器可挂载至玩家右手骨骼并显示。  
**剩余内容推迟至联机同步完成后再继续。**

- ✅ `ItemData` 扩展武器属性（攻击力、类型、攻击范围）
- ✅ 武器装备流程：`SlotComp` → `InventoryComp` → `Player.EquipWeapon` → 挂载 `BoneAttachment3D`
- ✅ `ItemComp` 装备状态下冻结物理、关闭碰撞、变为纯视觉 Mesh

- 🔲 武器 Hitbox 动态启用与伤害判定 —— **暂缓**
- 🔲 `SlotComp` 右键弹出上下文菜单（使用/装备/丢弃/拆分）—— **暂缓**
- 🔲 快捷键栏支持 —— **暂缓**
- 🔲 工具等级影响采集效率 —— **暂缓**

---

## 🔴 阶段 D：联机同步核心（当前最高优先级）
**立即启动，优先搭建网络底层，避免单机功能堆砌后的重构风险。**

- ✅ **基础网络连接**：两个客户端通过 IP 直连，验证场景同步  
  *（2026-05-07 测试通过：LAN 广播发现、主机创建/客户端加入、连接/断开事件均正常触发）*
- 🔲 **玩家位置/朝向/动画同步**（基于 `MultiplayerSynchronizer` 或自定义 RPC）
- 🔲 **`PlayerManager` 扩展为网络玩家管理**，区分本地玩家与远端玩家预制体
- 🔲 **容器操作网络化**：打开/关闭、物品转移封装为 RPC，服务器校验所有权后广播 `CustomData` 变更
- 🔲 **物品拾取/丢弃同步**：由服务器生成掉落物，客户端请求拾取，服务器处理库存变化
- 🔲 **攻击与伤害同步**：攻击发起 RPC，服务器计算伤害并广播结果
- 🔲 **植被采集、建造放置同步**（后续阶段 E 接入时直接使用网络层）

---

## 🔵 阶段 E：建造系统（联机同步完成后再启动）
**前置依赖**：阶段 C 采集系统剩余部分完成，阶段 D 联机框架稳定。

- 🔲 建造材料消耗：放置时从玩家背包扣除 `PlacedData` 中定义的所需物品
- 🔲 正式放置生成：实例化对应 `PlacedBase` 子类，自动分配唯一 `m_EntityGUID`
- 🔲 与持久化层集成：放置时注册到 `PlacedManager`，写入 `EntityRuntimeData`
- 🔲 建造预览增强：材料不足时显示红色/禁止放置
- 🔲 支持拆除建筑：移除节点，标记 GUID 为已删除，清理存档

---

## 🟣 阶段 F：实体属性与 AI 深化（远期）
- 🔲 属性系统正式设计（生命/耐力/力量/负重）
- 🔲 NPC 状态机扩展（战斗、死亡、逃跑）
- 🔲 NPC 尸体作为可搜索容器（掉落）
- 🔲 受伤反馈、击退

---

## ⚪ 远期规划
- 任务/剧情系统
- 动态难度与生态
- 完整联机房间管理
- 资源热加载与工具链
- 植被群落自动生成/刷新

---

**当前行动**：  
1. 创建基础网络测试场景，实现双客户端连接。  
2. 为 `Player` 添加位置/动画同步，验证玩家间可见性。  
3. 将 `ContainerComp` 核心操作改造为 RPC，确保容器同步正确。  
4. 逐步接入 `InventoryComp` 和攻击伤害的服务器校验。