# 📋 待开发任务清单

---

## 💾 1. 存档系统
- **NET 组件实现数据持久化**
  通过 `NetSyncBase` + `ISerializable` 将玩家/NPC/世界对象序列化为 JSON 写入文件。

- **玩家场景位置保存 (延后)**
  存档时记录玩家所在场景与坐标。

- **主机玩家场景读档**
  主机加载存档后恢复所有 NetObject 状态，客户端进服自动同步。

---

## 🌐 2. 网络相关
- **不同场景的同步分离 (延后)**
  当前全场景共用一套 RPC，未来按场景隔离网络对象。

---

## 🖥️ 3. 控制台逻辑修改
- **控制台生成物品需要网络注册**
  主机通过控制台生成物品时，需走 `NetObjectManager.SpawnObject` 流程，确保客户端同步。

---

## 📸 4. 拍照插件
- **完成拍照插件** (已完成)
  优先完成，用于朋友配合设定物品位置/场景参考。

---

## 🎮 5. Player 相关
- **PlayerAttack 加固**
  确保攻击逻辑仅在本地玩家所有（`IsOwner` 判断），防止客户端越权。

---

## 👤 6. NPC 相关
- **NPC 网络同步与本体解耦**
  挂载 `NetTransformSync` 到 NPC 预制体，主机 AI 驱动移动 → 客户端同步位置。

- **NPC 对话功能**
  交互触发对话，UI 显示文本，后续可扩展任务/交易。