个人优先级计划 2026.06.10
当前状态
ContainerComp 结构已按新设计更新，视图由 InventoryComp 内部管理，数据层具备自描述能力。但 m_inventoryView 尚未从 InventoryComp 获取赋值，导致箱子无法打开。所有旧版 RPC 方法保留，通信模型仍为“关箱全量提交”。

优先级 1（立即）—— 功能复原验证
目标：确保重构后的代码能完整运行原有的箱子交互流程。

补全 m_inventoryView 赋值 (m_inventoryView = m_InventoryComp.GetView())

修复 _Process 中视图引用 (m_inventoryView.GetParent())

本地单实例测试：打开/关闭箱子、放入/取出/整理物品，确认数据持久化正常

本地多实例联机测试：主机与客户端分别操作箱子，验证全量提交一致性

检查丢弃物品生成逻辑（当前可能缺失，需订阅 OnItemDropped 或保留原生成方式）

优先级 2（短期）—— RPC 模型升级
目标：在功能稳定的基础上，切换到“增删实时 + 关箱全量兜底”的混合同步模型，消除关键物品丢失风险。

在 IInventoryHolder 接口增加 IsRemoteContainer 属性

InventoryComp 增加/删除方法内集成自动 RPC 上报（客户端 + 箱子容器时触发）

ContainerComp 注册并实现 RPC_ContainerAddItem、RPC_ContainerRemoveItem

删除旧版 RPC：SubmitFinalInventory、SyncContainerOpenState、ReceiveCloseContainer 等

简化开关箱流程：主机不再直接操作 UI，通过 SyncContainerData 驱动客户端显示

联机测试增删实时性、关箱一致性、异常断线处理

优先级 3（中期）—— 背包系统网络化
目标：将玩家背包改造为主机权威实时同步，为后续属性/装备系统打下基础。

为 PlayerGUI 或 Player 添加背包同步组件 (NetSyncBase)

玩家背包增删移动均走实时 RPC，参照箱子混合模式

装备栏作为特殊 InventoryComp 纳入同步体系

优先级 4（长期）—— 跨容器交互与高级特性
目标：支持箱子与背包间的物品转移，完善世界交互体验。

实现跨容器转移 RPC（源容器移除 + 目标容器添加）

物品拾取、资源采集等操作接入 InventoryComp 统一逻辑

世界掉落物统一管理 (NetObjectManager)