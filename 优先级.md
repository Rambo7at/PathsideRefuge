# 🎯 近期开发优先级

**日期：** 2026.06.24  
**当前版本：** v2.4.1

---

## 📋 任务队列（按优先级排序）

### ⚙️ 阶段 A：生物类代码优化

| 序号 | 任务 | 说明 |
|------|------|------|
| A1 | `CreatureHitbox` → `CreatureAttackComp` | 类重命名，移至 `Comp` 文件夹，统一命名体系 |
| A2 | `Equipment` 精简 | 移除 `Humanoid` 中转属性（`m_EquipHitBox` / `m_EquipAttack`），直接注入碰撞盒给 `CreatureAttackComp` |
| A3 | `CreatureBase._EnterTree` 优化 | 将 `StateMachine`、`CreatureAttackComp` 等逻辑组件改为 `new` + `AddChild` 创建，消除场景手动拖拽依赖 |
| A4 | `StateMachine` Attack 卡死修复 | 增加攻击超时安全网，解决快速连击时状态机锁死问题 |
| A5 | 死亡/硬直状态统一处理 | 确保 `NpcAI`、`NpcMovement`、`PlayerController` 在死亡/硬直时正确停止逻辑 |

---

### ⚔️ 阶段 B：攻击基础完善

| 序号 | 任务 | 说明 |
|------|------|------|
| B1 | NpcAttack 组件 | NPC 主动攻击玩家，管理 `Area3D` 碰撞盒，复用 `CreatureAttackComp` 的判定逻辑 |
| B2 | 伤害公式接入 | `BaseDamage + 装备加成` 正式接入所有攻击流程，`FinalDamage()` 虚方法被子类实现 |
| B3 | 动物攻击支持 | 生物初始化时从 `CreatureData.EquipData` 读取天生武器（爪子/牙齿），走统一的 `CreatureAttackComp` 流程 |
| B4 | 攻击动画区分 | `AnimationTree` 根据武器类型参数切换攻击动画（空手/匕首/斧头） |

---

