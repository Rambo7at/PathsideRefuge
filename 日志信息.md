

# 开发日志 2026.05.24

## 🧱 容器库存持久化闭环
- ContainerComp：完善库存恢复与保存链路，_Ready 中从 PlacedData.m_data 恢复 InventoryData，通过 FlushInventory 在场景退出前回写。
- NetSyncBase 事件体系：引入 OnFlushNetState 事件，由 SceneBase 在保存前统一触发，确保各组件有最后机会刷新自定义数据。
- 对象翻倍修复：定位并解决因 NetObjectRegistry 未清理导致的场景对象倍增问题，在 SceneBase._EnterTree 中按场景 Hash 清理残留对象。
- 静态节点干扰处理：通过检查 NetObj 非空，防止场景内置静态物品执行容器初始化逻辑，避免空引用和重复 UI 绑定。

## 🛠 持久化系统完善
- WorldManager：拆分保存职责，PersistScene 负责将当前场景 NetObject 写入 WorldData，PersistCurrentScene 供上层调用，职责清晰。
- SceneBase：重构保存钩子，将原 SaveSceneObject 更名为 FlushNetStates，标记场景为非新建并触发组件状态刷新。
- NetSyncBase：将内部事件 OnSaveNetData 统一为 OnFlushNetState，与场景事件命名一致。
- PlacedData 数据流：确保 PlacedData 在建造时写入 NetObject.m_customData，读取时通过 DeepCopy 隔离运行时与存档数据。

## 📝 代码命名与日志优化
- SaveManager：方法重命名 —— LoadGameSaveData → Load，SaveGameSaveDataToLocal → Save，UpdateGameSaveData → PersistAll；日志补充玩家 ID、世界 ID、场景数量等关键信息。
- SceneBase：增强日志输出，记录场景名、Hash、是否新场景、恢复对象数量，便于追踪加载与恢复流程。
- CatLog 风格保持一致：所有日志沿用 [类名.方法名] 前缀，按严重程度使用 Info、Ok、Warn、Err、Debug。

## 🐞 问题修复
- 修复 SaveManager.PersistAll 在世界数据为空时的空引用崩溃。
- 修复 NetSyncBase 无订阅者时 OnFlushNetState?.Invoke() 避免 NullReferenceException。
- 修复 NetObject 缺失无参构造函数导致反序列化失败，添加 [GlobalClass] 与无参构造。
- 修复读档时 InvalidCastException（Resource 转 NetObject 失败），原因系旧存档残留，已清理并加入安全转换。





# 开发日志 2026.05.21

### 🌍 世界存档系统
- **SaveData**：清理冗余方法，新增 `m_worldDataDict` 与 `m_selworldIndex`，支持多世界选择
- **SaveManager**：扩展世界相关封装 —— `CreateWorld`、`GetAllWorldIDs`、`GetSelectedWorldData` 等，与玩家存档对称
- **WorldData**：新增世界数据类，通过名称设置时自动生成唯一 ID，支持 DeepCopy

### 🧩 场景与管理器解耦
- **WorldManager**：新建世界管理器，接管场景加载、相机挂载与场景数据持久化
- **SceneManager 旧逻辑迁移**：场景字典、`GetPackedScene` 场景实例化及类型校验移入 `WorldManager`
- **相机管理**：相机由 `WorldManager` 持有，根据场景类型（Game/View）自动挂载/卸载，绑定输入模式
- **场景切换安全修复**：解决旧场景销毁后访问 `_currentScene.Name` 导致 `ObjectDisposedException` 的问题，改为 `ChangeScene` 时提前记录日志，`SetCurrentSceneType` 不再触碰已销毁场景

### 💾 场景数据持久化
- **SceneBase**：`_EnterTree` 中从 `WorldManager.LoadSaveData` 加载存档数据并 DeepCopy，`_ExitTree` 中通过 `SaveSceneData` 写回 DeepCopy，形成独立存档副本
- **SceneData**：保留场景静态描述（名称、哈希、类型、网络对象数组），动态状态（如容器物品）后续通过 `NetObject` 或 `SceneData` 扩展字典承载
- **WorldManager 存取**：`LoadSaveData` 按场景哈希从 `WorldData.m_sceneDataDict` 获取或初始化，`SaveSceneData` 写入 DeepCopy 确保数据隔离

### 🌐 网络对象与自定义数据
- **PlacedData**：新增 `m_customData`（Variant）字段，用于存储容器库存等自定义数据，支持不同类型放置物自由扩展
- **NetObject**：已有 `m_customData` 字段，整体携带 `PlacedData` 作为网络同步的自定义数据
- **NetSyncBase**：利用 `SceneData.m_newScene` 判断场景是否新建，控制网络对象注册逻辑

### 🔧 细节优化
- 统一命名：视图类以 `View` 结尾，存档槽位组件命名规范等
- `PlacedData` 的 `m_customData` 改为 Variant 类型，避免编辑器中类型限制
- `WorldManager.GetCamera` 增加场景类型检查，非游玩场景返回 null
- 代码清理：移除多余的 `activeScene` 缓存，简化场景切换流程

---

下一步计划：补全世界选择 UI 回调，接入容器库存的持久化读写，测试“新建世界 → 放置容器 → 物品保存 → 读档恢复”完整闭环。



# 开发日志 2026.05.19

### Inventory
- 新增 Inventorydata 数据类
- 完成 Inventory 数据持久化基础

### SceneManager
- 新增 SceneManager
- 
--------------

# 开发日志 2026.05.17

### 📦 库存系统
- **InventoryComp**：重构为纯数据组件，移除 UI 依赖，统一通过数组管理物品数据
- **SlotView**：改造为数据直通视图，通过属性直接读写数据层，消除冗余数据副本
- **InventoryView**：分离为纯 UI 控制器，负责格子生成、事件转发与拖拽状态管理，不直接操作数据
- **拖拽交互**：重写拖拽逻辑，支持同一容器交换、跨容器传输及丢弃，交互完全内聚于 View 层
- **容器系统**：ContainerComp 适配新库存架构，箱子可独立挂载 InventoryComp 与 InventoryView，支持开关与距离检测


# 开发日志 2026.05.16

### 🌿 植被系统
- **TreeComp**：新增伤害接口，支持对树木造成伤害
- **TreeComp**：新增掉落物功能，树木被破坏后产生物品

### 🌐 网络层优化
- **GameCore**：场景切换时缓存当前场景引用，方便网络对象挂载
- **NetObjectManager**：重构对象生成流程 —— 在节点加入场景树前就设置好坐标，彻底解决生成时瞬移至原点再被物理弹飞的BUG
- **NetTransformSync**：代码优化，暂时移除平滑插值逻辑，专注处理权威位置更新

### 👤 NPC 系统
- **Npc**：移动逻辑修正，统一使用 -Z 轴为前方，与玩家模型保持一致

### 🎮 Player 系统
- **Player动画**：完成攻击与移动动画逻辑的统一，动画状态机现已正确衔接攻击与行走


--------------------





# 开发日志 2026.5.15

### 1：NetSync 组件
- 补发逻辑梳理：`NetObjectRegistry.OnPeerConnected` 向新客户端同步已有对象。
- NetSync 优化：`IsOwner` 属性增加空安全检查 (`m_NetObj != null && ...`)。
- 优化旋转同步：`NetTransformSync` 改为同步 `Player` 根节点的 `GlobalRotation`，移除对 `PlayerModel` 的依赖。

### 2：Player 组件
- 修复：`PlayerMoveAnimationDirection` 旋转对象从 `m_PlayerMesh` 改为 `m_player` 根节点，配合 `NetTransformSync` 正确同步朝向。
- 实现 Player HUD：`PlayerHud` 组件通过 `UIManager` 加载 `HudComp`，绑定血量并挂载到 `CanvasLayer`。
- 解耦 `PlayerController`：改为独立 `Node` 组件，通过 `NetSyncBase.IsOwner` 判断本地/远程，远程玩家不初始化控制器与 UI。
- 解耦 `PlayerCamera`：设置 `TopLevel = true`，手动跟随 `Player.GlobalPosition`，视角独立于角色旋转。
- 攻击状态优化：`EndAttack()` 根据当前速度直接切换到 `Idle` 或 `Walk`，消除中间帧闪烁。
- 动画状态机统一：攻击动画纳入 `AnimationTree` 状态机，通过 `not Attack` 条件返回移动子状态机。





---------------

# 开发日志 2026.5.13

### **Player 组件全面解耦**
- 解耦 `PlayerController`，改为独立 `Node`，挂载于 `Player` 根节点下，不再内嵌于 `Player` 类。
- 远程玩家对象通过 `NetSyncBase.IsOwner` 判断，非所有者不初始化控制器、UI 及物理处理。
- 状态机、视觉模型等子组件均解除对 `Player` 基类的直接依赖，只依赖根节点 `Node3D`。

### **旋转类型统一为 Vector3（欧拉角）**
- 彻底清理项目中所有 `Quaternion` 残留。
- `NetObject.Rotation` 字段、所有 RPC 方法参数（`Rpc_HostSyncRegister`、`Rpc_ReportToServer`、`Rpc_SyncToPeer`、`Rpc_SendTransform`）以及 `NetObjectManager` 设置旋转处均改为 `Vector3`。
- `RegisterObject`、`SpawnObject` 等调用点全部传入欧拉角，编译 0 错误。

### **日志系统 CatLog**
- 新建静态工具类 `CatLog`，提供分级日志：`Info`、`Ok`、`Warn`、`Err`、`Debug`。
- 支持按级别颜色输出与开关控制，已接入网络核心流程，替代零散 `GD.Print`。





  -------------





  -------------







# 途畔归所 · 开发日志（历史内容整合）

> 时间跨度：2026.3.6 – 2026.5.12  
> 涵盖架构搭建、单机功能、联机框架全部已完成里程碑。  

---

## 1：联机框架全面重构与完成（5月5日 – 5月12日）

### 场景与交互网络化（D2/D4）——基本完成，待收尾
- **场景管理**：预制体补发与客户端生成管道已调通，客户端玩家加入时主机通过 `Rpc_SyncToPeer` 补发，客户端正确创建实例。
- **断线清理**：已规划，待实现（当前 `OnPeerDisconnected` 中已预留日志，详细逻辑在下一步行动清单首位）。
- **交互网络化**：尚未启动，后续将按攻击 → 物品拾取 → 容器操作的顺序逐步 RPC 化。

### 玩家与实体同步（D3）——全部完成
- **`NetTransformSync`**：
  - 位置使用根节点，旋转支持分离的 `_rotMesh`（`m_PlayerModel`），不干扰摄像机。
  - 主机与客户端均实现双向上报与转发，主机在收到客户端上报时先更新本地镜像，再排除发送者转发给其他客户端。
  - 非 Owner 使用 `Lerp` 平滑插值，频率 20Hz，流畅无抖动。
- **`NetAnimationSync`**：
  - 通过同步 `StateMachine` 的整数值（`PlayerState` 枚举）驱动远程 `AnimationTree`。
  - 主机/客户端双向机制同上：客户端上报状态到主机，主机更新本地 `StateMachine` 并转发给其他客户端。
- **双客户端验证通过**：主机与任意客户端移动、攻击、跳跃，所有端均能实时看到位置、旋转、动画的正确同步。
- **所有权隔离**：`PlayerController` 入口添加 `if (player.m_PlayerData == null) return;`，杜绝远程角色被本地输入控制。

### 网络基础层（D1）——全部完成
- **`NetCore` 成型**：合并连接管理、LAN 广播发现（UDP）、ENet 连接、RPC 路由；提供 `ServerID = 1`、`LocalPeerID` 等核心属性。
- **`NetID` 与 `NetObject`**：分布式对象标识由 `UserID + 自增序号` 构成；`NetObject` 包含位置、旋转（最终统一为 `Vector3` 欧拉角）、预制体哈希、Owner PeerID。
- **对象注册与生成管道**：
  - `NetObjectRegistry` 单例管理所有 `NetObj`，主机分配 ID 并广播 `Rpc_HostSyncRegister`。
  - 客户端上报 `Rpc_ReportToServer`，主机再广播。
  - 新客户端加入补发全部现有对象（`Rpc_SyncToPeer`）。
- **`NetObjectManager`**：作为唯一实例化入口，维护预制体字典 `m_PrefabDict`，监听 `OnSpawned` 事件自动创建节点并挂载 `NetSyncBase`。
- **`NetSyncBase`**：挂载于每个网络节点，持有 `NetObject`，提供 `IsOwner` 判断，无信号设计。
- **远程屏蔽**：`Player._Ready` 中若 `m_PlayerData == null`（远程玩家）则关闭 `_Process`/`_PhysicsProcess`，不启用控制器、UI、摄像机。
- **旋转类型统一**：全链路将 `Quaternion` 旋转改为 `Vector3` 欧拉角，确保与 `GlobalRotation` 一致，同步更直接。

---

## 2：玩家状态机与战斗（5月初）

- **玩家状态机正式上线**：`StateMachine` 节点独立，定义 Idle/Walk/Jump/Fall/Attack 等状态，基于速度和地面状态自动切换。
- **攻击闭环**：通过 `AnimationNodeOneShot` 触发攻击动画，关键帧调用 `PerformAttack()` 射线命中并造成伤害，攻击期间锁定移动。
- **树木受击**：耐久通过 `CustomData["durability"]` 管理，避免污染静态 Resource。
- **状态机与移动解耦**：`PlayerController` 中增加攻击状态判断，攻击时禁用移动但不影响重力。

---

## 3：实体与 AI 基础（4月底 – 5月初）

- **Player 重构**：`PlayerUIHandler` 接管所有 UI 管理，`Player` 仅保留交互路由，减少约 80 行代码，为网络层接入铺路。
- **NPC 继承统一**：废弃旧 `NpcBase`，`Npc : Humanoid` 完全融入实体体系，动画树自动响应移动状态。
- **巡逻与导航**：纯直线巡逻系统落地（极坐标随机选点，可配置参数）；导航网格全局绘制并烘焙，预留 `NavigationAgent3D` 寻路接口。
- **视线与追击**：`m_eye` 升级为 `RayCast3D`，`See()` 检测触发巡逻→追击切换，目标丢失后自动回退。
- **状态机雏形**：Npc  Patrol/Chase 状态调度，`_PhysicsProcess` 极简化为四步流程。

---

## 4：物品与背包系统（4月）

- **物品系统数据驱动**：`ItemComp` 仅持有 `[Export] ItemData`，物品属性完全配置化；`ItemData` 为可编辑 `Resource`，支持中文分组。
- **背包核心逻辑**：`SlotComp` + `InventoryComp` 实现格子自治、堆叠、拖拽交换、拆分，严格遵循数据/表现分离。
- **背包持久化闭环**：读档自动实例化独立格子，回填存档数据；`RefSlot()` 实时同步存档，实现“操作即存档”。
- **交互优化**：移除旧版检测区域，统一使用 `m_eye`（RayCast3D）进行射线拾取、攻击检测。
- **放置物与容器**：建立 `PlacedBase` 继承体系，`PlacedData` 定义类型枚举；`ContainerComp` 实现 `IInventoryHolder`，支持 E 键开关、跨库存拖拽、持久化容器物品。
- **容器 UI 完善**：复用 `InventoryComp` 双面板，支持拖拽转移与同库存交换，预留过滤器接口。

---

## 5：架构与框架奠基（3月 – 4月初）

- **基础架构确立**：搭建 `GameCore`、`NetworkCore`（早期ENet P2P）、`UIPanelBase`，确立单例管理器、无全局静态、服务定位器模式。
- **实体继承体系**：建立 `CreatureBase → Humanoid → Player` 以及 `CreatureBase → Humanoid → Npc` 双线，统一移动、重力、动画树、视线等基础设施。
- **数据表现分离铁律**：移除 `PlayerData` 对场景节点的直接引用，确保存档安全、可网络同步；引入 `PlayerManager` 统一管理玩家实例。
- **资源管理器**：实现按脚本类型自动分类加载预制体，支持 `Player`、`ItemComp`、UI 面板等快速获取。
- **存档系统**：`SaveData` 采用 `Dictionary<int, PlayerData>` 结构，支持多角色选择、增删存档；`PlayerData` 继承 `Resource`，内置背包数据，全程深拷贝保护。
- **初始化流程**：解决 `AddChild(null)`、`private set` 编译错误等时序问题，确保所有管理器 `Init()` 显式调用，依赖顺序可控。

---