## ---------- 2026.5.4 开发日志 ----------

### 物品拾取：射线检测重构
- ✅ 移除旧的“检测区域 + 距离最近”拾取模式，改用 `m_eye` (RayCast3D) 直接射线检测
- ✅ `Player.Test()` 内实现：射线命中 `ItemComp` 时直接调用 `PlayerInteract`，简化交互流程
- ✅ 拾取逻辑与 E/F 双键交互统一，更精确、更即时

### 建筑放置物组件初建
- ✅ 新增 `PlacedComp` (StaticBody3D)，挂载 `PlacedData`，作为已放置建筑的基础实例
- ✅ `PlacedData` 补充类型枚举：建筑、容器、工作台，为后续放置物扩展提供数据骨架

### 建筑部件管理器上线
- ✅ 新增 `PlacedManager`，负责 `PlacedData` 预制件的加载与实例化
- ✅ `ResourceManager` 资源归类接入 `PlacedComp`，资源分类完成闭环
- ✅ `GetPiece()` 可复用获取建筑预览/放置模型，为建筑系统提供基础服务













## ---------- 2026.5.3 开发日志 ----------

### 基类能力增强
- ✅ `CreatureBase` 新增 `ApplyGravity` / `MoveHorizontally`，移动执行上提
- ✅ `CreatureBase` 新增 `FaceMovementOrTarget`，智能转向（移动朝速、静止朝目标）

### 导航寻路正式启用
- ✅ 巡逻接入 `NavigationAgent3D`：生成目标点同时设置 `TargetPosition`，移动跟随路径点
- ✅ 追击接入导航：玩家位置投影到导航网格，设置 `TargetPosition`
- ✅ 修复追击无导航线：统一实时获取地图 `Rid`

### 视线检测硬件化
- ✅ `m_eye` 从 `Marker3D` 换为 `RayCast3D`，运行时可见射线
- ✅ `See()` 测试通过，命中 `Player` 进入追击

### 状态机初建
- ✅ `enum NpcState { Patrol, Chase }`，`UpdateStateMachine` 统一调度
- ✅ `See()` 触发追击，目标失效自动回退巡逻

### 阵营系统基础
- ✅ `NpcDisposition` 枚举加入 `NpcData`，默认中立

### 流程极简
- ✅ `_PhysicsProcess`：`重力 → 状态调度 → 移动应用 → 物理滑动 → 智能转向`








## ---------- 2026.5.1 开发日志 ----------

### NPC 继承体系重构
- ✅ 废弃旧 `NpcBase : CharacterBody3D`，新建 `Npc : Humanoid`，扎根实体体系
- ✅ 继承链统一：`Npc → Humanoid → CreatureBase`，复用 `IsMoving`、`m_AnimationTree`、`m_eye` 等全部基础设施
- ✅ 动画树自动响应：`IsMoving` 只读属性基于 `Velocity` 计算，巡逻时自动切换待机/跑步

### NPC 移动系统封装
- ✅ `_PhysicsProcess` 瘦身为四行流程：`UpdateGravity` → `UpdatePatrolLogic` → `ApplyMovement` → `SmoothRotate`
- ✅ `ApplyMovement` 独立处理水平速度与朝向角，方向来源可替换（直线/导航）
- ✅ `SmoothRotate` 平滑旋转整个 NPC，`m_eye` 跟随转身，告别侧滑
- ✅ 基于 `NpcData` Resource 参数化：`m_speed`、`m_patrolRadius`、`m_stopTime`、`m_targetDistance` 均可配置

### 巡逻系统实现
- ✅ 纯直线巡逻：围绕 `originPoint` 极坐标随机选点，到达后停留 `m_stopTime` 秒
- ✅ 无导航依赖：消除 `NavigationAgent3D` 可达性失败导致的原地静止问题
- ✅ 自动落地等待：空中出生时暂不记录原点，首次 `IsOnFloor()` 后再开始巡逻

### 导航系统接入
- ✅ `MainWorld._Ready()` 中基于 Terrain3D 导航绘制 + 编辑器烘焙生成全局 `NavigationMesh`
- ✅ `Npc.TryInitialize()` 中添加 `TestNavigationAvailability()` 检测导航地图有效性
- ✅ 验证通过：导航地图 RID 有效，自身位置可达，为后续寻路避障铺路
- ✅ 预留 `GetNavigationDirection()` 方法，一行替换即可从直线巡逻切换到导航寻路

### 核心设计决策
- ✅ 导航网格采用“全图涂蓝”策略，避免 NPC 离开导航区域后行为异常
- ✅ 双层导航 + 旅行成本控制作为后续 NPC 优先走主干道的规划方案（待实现）
- ✅ `NavigationAgent3D` 节点保留但暂不启用寻路，直线移动阶段先跑通基础行为

### 代码清理与命名规范化
- ✅ 全项目类名、方法名统一命名风格，移除冗余与临时测试代码
- ✅ 补全关键方法注释，明确职责与参数含义
- ✅ 清理调试阶段遗留的废弃字段与无效引用，提升代码可读性

### 架构收益
- ✅ Npc 类职责清晰：初始化 / 巡逻决策 / 移动执行 / 旋转 各方法独立
- ✅ 与 Player 共享 `Humanoid` 移动模板，后续 Player 重构时可统一移动物理
- ✅ 导航系统验证完成，随时可切换到完整寻路 + RVO 避障模式




## ---------- 2026.4.30 开发日志 ----------

### 服务定位器与单例化
- ✅ 剩余非 Node Manager（`ItemManager`、`PlayerManager`、`UIManager`）全部静态单例化，全局 `Instance` 直达
- ✅ Node 型 Manager（`TimeManager`、`ConsoleManager`、`NetworkCore`）手动赋值 + AddChild 控制顺序，确保树构建前可用
- ✅ `GameCore` 瘦身，仅保留初始化顺序与节点追加，不再作为访问中介
- ✅ `SaveManager`、`ResourceManager` 优先单例化，采用显式 `Init()`，消除构造时序依赖

### 资源管理器重构
- ✅ `ResourceManager` 按脚本类型（`is Player`、`is ItemComp`、`is UIPanelBase`）自动分类，存入 `m_PlayerPrefab`、`m_ItemPrefabs`、`m_UIPrefabs`
- ✅ 终结遍历全列表误传资源模式，`ItemManager.Init()` 直接索取分类好的列表

### 初始化流程修复
- ✅ 解决 `private set` 导致无法手动赋值的编译错误，统一改为 `{ get; set; }`
- ✅ 修复 `AddChild(null)` 崩溃：Node 单例先 new → 手动设 Instance → 再 AddChild，杜绝未就绪引用
- ✅ 补全 `ResourceManager` 中列表初始化，消除 `NullReferenceException`

### 物品系统数据驱动重构
- ✅ `ItemComp` 移除全部离散导出字段，仅持有 `[Export] ItemData`，表现与数据彻底分离
- ✅ `ItemData` 为 `Resource`，可直接在编辑器中创建、配置；使用 `ExportGroup` 添加中文分组，降低团队配置门槛
- ✅ 调整 `PickUp`、`Unbox` 等逻辑，统一基于 `ItemData` 操作

### 存档系统升级与背包持久化
- ✅ `SaveData` 玩家列表改为 `Dictionary<int, PlayerData>`，提供按 ID 高效查找
- ✅ `PlayerData` 继承 `Resource`，内置 `Array<SlotData> m_InventoryData`，背包数据融入存档结构
- ✅ 提供 `DeepCopy()` 封装，获取玩家数据副本，防止污染原始存档
- ✅ `InventoryComp` 在恢复时基于 `PlayerData.m_InventoryData` 深拷贝重建格子，加载安全
- ✅ `RefSlot()` 统一刷新并同步存档，实现“操作即存档”，`AddItem` 堆叠与占格逻辑修复

### 场景切换与交互修复
- ✅ 修正 `MainMenu.LocalGame()` 中遗漏 `return` 导致一次调用执行两次场景切换的致命错误，消除 `GetTree()` 空引用崩溃
- ✅ `SlotComp.DropItem()` 完成：安全拷贝物品数据 → 生成掉落物 → 放置于玩家视线前方 → 清空格子并同步存档

### 存档选择 UI 组件化
- ✅ 新建 `PlayerSaveSlotComp`，封装存档按钮逻辑，保留 `PlayerID`，编辑器绑定选择信号
- ✅ `RefreshSaveBox` 动态生成按钮列表，`SaveBoxArray` 管理生命周期，支持多存档切换

### 架构成果与风格确立
- ✅ 完全进入“服务定位器”模式，访问路径统一为 `Manager.Instance`
- ✅ 显式 `Init()` 初始化风格贯穿全项目，依赖顺序清晰可控
- ✅ 资源自动归类、数据驱动配置，为后续商店、合成等系统奠定坚实基础













## ---------- 2026.4.29 开发日志 ----------
### 架构优化 · 消除数据层对表现层引用，存档系统健壮性升级
- ✅ 移除 `PlayerData` 中对 `Player` 节点的直接引用（`m_LocalPlayer`），彻底解决存档序列化风险、内存泄漏隐患及跨场景引用崩溃问题
- ✅ 确立“数据表现分离”铁律：`PlayerData` 作为纯数据 `Resource`，不再持有任何场景节点，确保存档安全、可网络同步
- ✅ 新增 `PlayerManager.GetLocalPlayer()` 方法，通过 `ActivePlayers` 字典安全获取本地玩家节点，替代原先危险的数据内反向引用
- ✅ 统一全局访问路径：所有组件（如 `SlotComp`）通过 `PlayerManager.GetLocalPlayer()` 获取节点，消除空引用崩溃

### 存档选择系统 · ID驱动索引替代位置索引
- ✅ 重构 `SaveData` 选择逻辑：用 `PickPlayer`（玩家ID）替代 `index`（数组索引），支持存档增删后仍能准确定位当前选中角色
- ✅ 实现 `GetPickPlayerData()` 安全查询，自动处理空存档、ID失效回退，保证返回值永远有效
- ✅ 实现 `PickNextPlayer()` 切换逻辑，利用 Godot `Array.IndexOf` 实现高效循环选择，支持无缝切人
- ✅ 移除 UI 中直接修改全局 `index` 的脆弱属性，改为本地缓存+确认提交模式，防止误修改

### Bug修复与健壮性强化
- ✅ 修复 `SlotComp.DoDrag` 中因 `PlayerData` 持有无效节点引用导致的 `NullReferenceException`
- ✅ 为所有跨模块调用添加空引用防御，保证在资源未就绪时不会崩溃
- ✅ 梳理并统一数据流：`PlayerData` ↔ `PlayerManager` ↔ 表现层，实现单向依赖、易于测试

### 整体里程碑
- ✅ 数据层（`PlayerData`、`SaveData`）彻底解耦表现层，具备联网、多存档扩展能力
- ✅ 存档选择系统健壮，支持动态增删存档、多角色切换，逻辑清晰可维护
- ✅ 框架核心原则（服务定位器+数据分离）落地深入，所有改动均严格遵循架构规范

## ---------- 2026.4.14 开发日志 ----------
### 背包系统 · 存档读档闭环与持久化完整落地
- ✅ 彻底修复读档后物品无法交换问题，统一格子创建流程，强制唯一 SlotID 0~9，杜绝ID重复与引用共享
- ✅ 优化背包数据刷新逻辑，采用打开背包时手动调用 RefSlot()，解决 _Ready() 刷新时机过早、UI不显示问题
- ✅ 完善 InventoryComp 读档逻辑，先实例化独立格子，再回填存档物品数据，全程使用深拷贝保证数据安全
- ✅ 验证全流程稳定：物品添加、拖拽、交换、堆叠、保存、读档、重新打开全部正常无BUG
- ✅ 严格遵循项目架构：数据表现分离、单例管理器、无全局静态、格子自治、玩家绑定规范

### 框架稳定与细节优化
- ✅ 清理管理器构造函数业务逻辑，规避数组越界、资源加载空引用等启动崩溃问题
- ✅ 完善存档文件存在性判断，避免启动时 Missing File 报错
- ✅ 统一代码注释规范，保持与项目整体风格一致
- ✅ 背包系统完全独立、可复用、无耦合，符合框架设计标准

### 整体里程碑
- ✅ 背包系统功能 + 数据持久化 + 存档读档 完整闭环
- ✅ 成为项目首个全流程稳定、可上线使用的业务模块
 --------------------
## ---------- 2026.4.11 开发日志 ----------
### 背包系统 · 数据持久化与存档闭环完成
- ✅ 修复存档数据读取为空问题，添加 [Export] 与 [GlobalClass] 实现正确序列化
- ✅ 重构 InventoryComp，新增 BindPlayer 绑定玩家，实现数据一一对应
- ✅ 实现存档自动加载、SaveData 同步、RefSlot 自动保存，完成运行时→存档闭环
- ✅ 全面使用深拷贝，杜绝引用错乱，保证数据安全
- ✅ 验证全流程：添加、拖拽、交换、存档、读档完全正常

### 代码规范与结构优化
- ✅ 统一类结构排序与注释规范
- ✅ 清理冗余代码，优化变量与私有成员管理
- ✅ 严格遵循表现/数据分离架构

### 框架稳定与功能闭环
- ✅ 背包系统功能完全闭环，稳定可用
- ✅ 严格遵守项目单例、无静态、格子自治规范

## ---------- 历史内容（2026.3.6 - 2026.4.10） ----------
- 核心架构：搭建 GameCore、NetworkCore（ENet P2P 局域网联机）、UIPanelBase 基础框架，确立单例管理器、无全局静态规范。
- 实体系统：建立 CreatureBase→Humanoid→Player 继承体系，实现生命、受击、死亡及网络权威控制，完成 Player 与 PlayerController 职责分离。
- 主菜单与控制台：完成主菜单 UI 切换、存档检测、局域网联机功能；开发 ConsoleComp+ConsoleManager，实现基础命令解析与物品生成对接。
- 背包系统（前期）：完成 SlotComp、SlotData 开发，实现格子自治、无冗余拖拽、物品交换功能，统一命名规范，清理冗余代码，确立 Resource 存档基础规则。
- 物品与互动：完成物品系统、拾取功能，开发快递盒、BoxComp 存储箱基础组件；完善背包交换、拆分逻辑，搭建建造系统基础。