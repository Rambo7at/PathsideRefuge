# 开发日志汇总

## 2026.08.18
```
1. 修复箱子闪退问题（核心）
问题现象：主机离开场景去 B 后，客户端在场景 A 中打开箱子会闪退

排查路径：
  - 发现 CustomData setter 中的 OnDataChanged?.Invoke() 导致重入，删除后闪退解决
  - 发现 byte[] 在 RPC 参数中序列化不稳定，改用 Variant 包装传递
  - 发现 RequestCustomData 和 SubmitCustomData 中 IsDataSyncing 锁的解锁顺序问题，调整为先解绑和解锁，再执行回调

最终结果：箱子在主机/客户端、同场景/跨场景所有场景下均正常工作，版本号正确递增

2. 重命名 NetObjectManager → NetObjectInstance
职责明确：管理场景中的 Node 实例，与 NetObjectRegistry（数据管理）区分
类名、文件名、所有引用全部更新

3. SceneOwnerManager 断线处理优化
新增 _playerSceneDict（玩家 PeerID → 所在场景哈希）
新增 HandlePlayerDisconnected 方法：断线时清理玩家场景映射，若是场景拥有者则触发所有权转移
新增 IsPlayerInScene / IsPlayerConnected 辅助方法
Rpc_RequestSceneOwnership 中记录玩家所在场景

4. 文档更新
项目结构速览同步更新，反映 NetObjectManager → NetObjectInstance 重命名
目录结构改为表格形式，提升可读性

5. 3人联机测试
主机 + 两个客户端同场景测试通过
断线处理基础通路正常工作
位置同步有小问题，暂缓处理
```






## 2026.08.17
```
🧠 场景拥有者管理器（SceneOwnerManager）完整重构

用户对 SceneOwnerManager 进行了全面重构和命名优化：

核心改动：
  - 移除 TaskCompletionSource 异步机制，改为 RPC 直连模式
  - 删除 Rpc_ReceiveAllOwners / Rpc_RequestOwners / Rpc_TakeOwnershipNotification
  - 新增 Rpc_RequestSceneOwnership / Rpc_GrantSceneOwnership / Rpc_TransferOwnership
  - 新增 Rpc_QuerySceneActive（原 Rpc_问场景是否还有存活者）
  - 统一命名规范：所有 RPC 方法使用英文，简洁直观

所有权转移链路：
  拥有者离开 → TransferOwnership → Rpc_TransferOwnership（服务器）
  → _sceneOwners.Remove(sceneHash)
  → BroadcastQuerySceneActive → 询问其他客户端
  → 存活者收到 Rpc_QuerySceneActive → RequestSceneOwnership
  → 无主场景 → 分配新拥有者 ✅

主机夺取逻辑：
  有主场景 + 主机申请 → 主机夺取拥有权 → BroadcastQuerySceneActive
  有主场景 + 普通客户端 → 回复当前拥有者（不覆盖）

命名优化：
  Rpc_问场景是否还有存活者 → Rpc_QuerySceneActive
  BroadcastQueryOccupants → BroadcastQuerySceneActive
  OnOwnershipAcquired → OnOwnershipGranted
  删除 Rpc_NotifySceneOwnership（空实现）


📦 场景数据存储机制改造

SceneData 序列化方法移除：
  - 删除 ISerializable 接口实现
  - 删除 Serialize() / Deserialize() 方法
  - 删除 SceneDataDto 结构体
  - SceneData 现在只保留纯数据字段

WorldManager.SaveSceneData 重写：
  旧方式：依赖 SceneBase.SaveAllStates() 收集数据
  新方式：直接从 NetObjectRegistry.GetNetObjectsDict() 获取数据
  按 SceneHash 分组 → 更新 CurrentWorld.SceneDataDict

LoadSceneData 逻辑修正：
  - 移除 SceneData 覆盖逻辑，只更新 NetObjectList
  - 保留原有 SceneData 引用（含正确的 SceneHash）
  - 解决哈希值被覆盖为 0 的问题


🐛 关键 Bug 修复

NetID.SceneHash 为 0 的问题：
  根因：RestoreNetObjects 中 SceneData = sceneData 覆盖了正确的哈希值
  修复：改为只更新 NetObjectList，不替换整个 SceneData 引用

场景切换后旧场景数据残留：
  根因：NetObjectManager._netObjectInstances 未清理
  修复：_ExitTree 中调用 RemoveObject 清理实例

客户端镜像在服务端不生成：
  根因：Rpc_ReportToServer 未将 NetID 加入 _sceneNetObjectsList
  修复：同步更新 _sceneNetObjectsList 索引

跨场景 RPC 导致 ObjectDisposedException：
  根因：客户端发送的位置同步 RPC 到达已切换场景的主机
  修复：Rpc_Reliable / Rpc_Unreliable 增加 sceneHash 校验
  if (WorldManager.Instance.CurrentSceneHash != sceneHash) return;

RemoveNet 广播机制改造：
  去中心化广播：任何端调用 RemoveNet 都会广播给所有其他客户端
  Rpc_DestroyNetObject 接收端处理：本地删除，不广播


🏗️ NetSyncBase 生命周期管理

_ExitTree 统一清理：
  - 所有对象离开场景时调用 RemoveObject 清理实例
  - Player 对象由 Player._ExitTree 中的 RemoveNet 处理
  - RemoveObject 内部有 TryGetValue 检查，幂等安全

RegisterManual 逻辑完善：
  - 非拥有者 + NetID 为空 → 销毁本地预置，等待服务器下发
  - 新场景 + NetID 为空 → 注册自身
  - NetID 仍为空 → 销毁

```



----------
 
# 途畔归所 · 开发日志（历史内容整合）
> 时间跨度：2026.5.13 – 2026.8.15  
```
🌐网络层&NetObject体系
- NetObject重构：m_customData由Variant改为byte[]，彻底解决EncodedObjectAsId序列化崩溃；移除NetDataType枚举，上层泛型做类型校验；NetID语义升级，OwnerPeerID重命名为PeerID，区分【创建者】与【场景拥有者】两套概念。
- 新增NetDataPackage统一封包：Ver版本、UserLockId交互锁、GlobalFlag全局标记、Data业务载荷，箱子、门、电梯等可交互对象复用此封包。
- 三段式自定义数据RPC协议：Rpc_RequestCustomData / Rpc_ReceiveCustomData / Rpc_SubmitCustomData / Rpc_AcknowledgeCustomData；DataRevision版本号机制，提交冲突时服务器拒绝旧版本、回刷客户端。
- UserLockId通用交互锁，无需解析业务数据即可判断对象是否被占用。
- RPC调用方式重构：废弃bool reliable重载，改为SendXxx可靠 / SendFastXxx快速两套接口；RpcGateway定位轻量路由器，对象级RPC交给NetSyncBase，场景级RPC交由WorldManager；RPC增加IsInit、sceneHash校验，过滤跨场景无效消息，避免ObjectDisposedException。
- NetSyncBase改造：NetObj改为只读属性动态从Registry获取；移除IsInit字段；RegisterManual注册逻辑完善；RequestCustomData改为Action事件回调消除async污染，SubmitCustomData增加3s超时保护、IsDataSyncing防重入锁。
- NetObjectRegistry完善RemoveNet去中心化广播机制，_ExitTree生命周期统一做对象注销清理，幂等安全。

🧠场景所有权体系演进
- 08.12：完成SceneOwnerManager单例，实现场景Hash‑OwnerPeerID字典；实现所有权转移完整RPC链路；SceneBase接入所有权，主机/客户端进入、退出场景分别执行所有权申请、移交逻辑。
- 08.15：旧TaskCompletionSource异步请求机制移除，为08.18版本做前置铺垫。

📦容器&库存系统
- ContainerComp完整重构：主机客户端统一RPC入口CallLocal=true；删除本地权限分支，全部走RPC；权限由IsOwner权威端 + UserLockId占用锁双重控制；废弃DeepCopy，直接调用InventoryData.Serialize持久化，修复IsOpen、InteractPeer丢失问题；打开/关闭UI完整回调链路，Submit确认回传，解决异步加载空UI、超时问题。
- 验收：主机/客户端开关箱子、并发操作、版本防覆盖、超时保护全部通过。

🗡️装备、战斗、动画系统
- 装备底层：EquipData由数组改为Dictionary<E_EquipAVL,ItemData>；实现双手互斥、攻击状态拦截换装；主副手HitBox独立事件绑定，支持双刀交替攻击判定；ItemData新增DualAttackAnimIndex，自动切换双持动画。
- 盾牌防御闭环：点积做正面格挡判定；RequestDefense+Tween平滑防御混合；副手盾牌优先级高于单手主手，双手武器不可格挡；输入做防重复调用保护。
- StateMachine统一：合并玩家与NPC状态机，移除NetAnimationSync，动画状态直接由NetSyncBase调用RPC双向同步；移动状态只在变更时发送RPC降低带宽，OneShot/连段使用可靠通道；修复主机收不到自身动画同步问题。
- 连段攻击：CreatureAnimComp动画帧委托驱动Combo窗口；窗口内预输入，动画结束帧消费连段信号，支持多段连击。
- NPC战斗：NpcBattle组件封装攻击冷却、招式选择；攻击状态禁止移动；修复组件初始化空引用；SenseComp射线感知优化，复用物理射线实例，NPC感知与玩家交互共用；导航增加迭代ID校验，规避导航地图未就绪报错。
- 生物体系：CreatureData统一合并PlayerData与NpcData；硬直系统基于伤害阈值触发；植被TreeComp实现IDamageable，联机砍树、概率掉落完整跑通。

👤玩家、管理器与持久化
- 管理器拆分：SaveManager降级纯IO服务，运行时数据剥离；WorldManager接管世界、场景数据；PlayerManager接管玩家数据；数据流转统一DeepCopy导出再落盘。
- 玩家存档扩展：LastSceneHash、LastPosition、LastRotation，支持跨场景角色位置恢复；EnterGame统一游戏入口；PersistAll简化为两行管理器保存调用。
- 跨场景切换全链路完成：CallDeferred延迟切换规避物理回调冲突；玩家自动重生实例、相机归还；装备、背包数据跨场景保留。
- UI视图层重构：InventoryView、SlotView职责拆分，数据‑表现分离；PlayerUIHandler重命名PlayerGUI，HUD合并；拖拽交互重写，支持同容器、跨容器物品交换、丢弃；丢弃物品走NetObjectManager网络生成流程。

🐛主要已修复Bug
1. EncodedObjectAsId序列化崩溃：CustomData切换byte[]
2. Submit无确认导致超时：新增Rpc_AcknowledgeCustomData回传
3. DeepCopy丢失非Export字段：取消DeepCopy，直接调用Serialize
4. 客户端打开箱子空数据：改为事件回调，数据就绪后刷新UI
5. 主机RPC无法发给自己：统一CallLocal=true调用模式
6. InteractPeer锁丢失：完整序列化到DTO
7. 跨场景RPC异常：增加sceneHash过滤校验
8. NetID.SceneHash被覆盖为0：LoadSceneData只更新NetObjectList不替换SceneData引用
9. 场景切换旧网络对象残留：_ExitTree统一调用RemoveObject清理实例

📋待办清单(P1优先)
- 玩家断线PeerDisconnected场景所有权处理
- 门、电梯、工作台等通用可交互对象扩展
P2：传送场淡入淡出过渡；地面物品场景切换持久化；玩家背包、血量、位置服务器持久化

📈版本里程碑
M6：玩家数据持久化与入口统一 v2.7.4；M7：管理器架构重构与数据持久化闭环 v2.7.5；整体进度72%
```



----

> 时间跨度：2026.3.6 – 2026.5.12  
> 涵盖架构搭建、单机功能、联机框架全部已完成里程碑。  
 ```
## 1：联机框架全面重构与完成（5月5日 – 5月12日）

### 场景与交互网络化（D2/D4）——基本完成，待收尾
- **场景管理**：预制体补发与客户端生成管道已调通，客户端玩家加入时主机通过 `Rpc_SyncToPeer` 补发，客户端正确创建实例。
- **断线清理**：已规划，待实现（当前 `OnPeerDisconnected` 中已预留日志，详细逻辑在下一步行动清单首位）。
- **交互网络化**：尚未启动，后续将按攻击 → 物品拾取 → 容器操作的顺序逐步 RPC 化。

### 玩家与实体同步（D3）——全部完成
- **`NetTransformSync`**：
  - 位置使用根节点，旋转支持分离的 `_rotMesh`（`m_PlayerModel`），不干扰摄像机。
  - 主机与客户端均实现双向上报与转发，主机在收到客户端上报时先更新本地镜像，再排除发送者转发给其他客户端。
  - 非 Owner 使用 `Lerp` 平滑插值，频率 20Hz，流畅无抖动。
- **`NetAnimationSync`**：
  - 通过同步 `StateMachine` 的整数值（`PlayerState` 枚举）驱动远程 `AnimationTree`。
  - 主机/客户端双向机制同上：客户端上报状态到主机，主机更新本地 `StateMachine` 并转发给其他客户端。
- **双客户端验证通过**：主机与任意客户端移动、攻击、跳跃，所有端均能实时看到位置、旋转、动画的正确同步。
- **所有权隔离**：`PlayerController` 入口添加 `if (player.m_PlayerData == null) return;`，杜绝远程角色被本地输入控制。

### 网络基础层（D1）——全部完成
- **`NetCore` 成型**：合并连接管理、LAN 广播发现（UDP）、ENet 连接、RPC 路由；提供 `ServerID = 1`、`LocalPeerID` 等核心属性。
- **`NetID` 与 `NetObject`**：分布式对象标识由 `UserID + 自增序号` 构成；`NetObject` 包含位置、旋转（最终统一为 `Vector3` 欧拉角）、预制体哈希、Owner PeerID。
- **对象注册与生成管道**：
  - `NetObjectRegistry` 单例管理所有 `NetObj`，主机分配 ID 并广播 `Rpc_HostSyncRegister`。
  - 客户端上报 `Rpc_ReportToServer`，主机再广播。
  - 新客户端加入补发全部现有对象（`Rpc_SyncToPeer`）。
- **`NetObjectManager`**：作为唯一实例化入口，维护预制体字典 `m_PrefabDict`，监听 `OnSpawned` 事件自动创建节点并挂载 `NetSyncBase`。
- **`NetSyncBase`**：挂载于每个网络节点，持有 `NetObject`，提供 `IsOwner` 判断，无信号设计。
- **远程屏蔽**：`Player._Ready` 中若 `m_PlayerData == null`（远程玩家）则关闭 `_Process`/`_PhysicsProcess`，不启用控制器、UI、摄像机。
- **旋转类型统一**：全链路将 `Quaternion` 旋转改为 `Vector3` 欧拉角，确保与 `GlobalRotation` 一致，同步更直接。

---

## 2：玩家状态机与战斗（5月初）

- **玩家状态机正式上线**：`StateMachine` 节点独立，定义 Idle/Walk/Jump/Fall/Attack 等状态，基于速度和地面状态自动切换。
- **攻击闭环**：通过 `AnimationNodeOneShot` 触发攻击动画，关键帧调用 `PerformAttack()` 射线命中并造成伤害，攻击期间锁定移动。
- **树木受击**：耐久通过 `CustomData["durability"]` 管理，避免污染静态 Resource。
- **状态机与移动解耦**：`PlayerController` 中增加攻击状态判断，攻击时禁用移动但不影响重力。

---

## 3：实体与 AI 基础（4月底 – 5月初）

- **Player 重构**：`PlayerUIHandler` 接管所有 UI 管理，`Player` 仅保留交互路由，减少约 80 行代码，为网络层接入铺路。
- **NPC 继承统一**：废弃旧 `NpcBase`，`Npc : Humanoid` 完全融入实体体系，动画树自动响应移动状态。
- **巡逻与导航**：纯直线巡逻系统落地（极坐标随机选点，可配置参数）；导航网格全局绘制并烘焙，预留 `NavigationAgent3D` 寻路接口。
- **视线与追击**：`m_eye` 升级为 `RayCast3D`，`See()` 检测触发巡逻→追击切换，目标丢失后自动回退。
- **状态机雏形**：Npc  Patrol/Chase 状态调度，`_PhysicsProcess` 极简化为四步流程。

---

## 4：物品与背包系统（4月）

- **物品系统数据驱动**：`ItemComp` 仅持有 `[Export] ItemData`，物品属性完全配置化；`ItemData` 为可编辑 `Resource`，支持中文分组。
- **背包核心逻辑**：`SlotComp` + `InventoryComp` 实现格子自治、堆叠、拖拽交换、拆分，严格遵循数据/表现分离。
- **背包持久化闭环**：读档自动实例化独立格子，回填存档数据；`RefSlot()` 实时同步存档，实现“操作即存档”。
- **交互优化**：移除旧版检测区域，统一使用 `m_eye`（RayCast3D）进行射线拾取、攻击检测。
- **放置物与容器**：建立 `PlacedBase` 继承体系，`PlacedData` 定义类型枚举；`ContainerComp` 实现 `IInventoryHolder`，支持 E 键开关、跨库存拖拽、持久化容器物品。
- **容器 UI 完善**：复用 `InventoryComp` 双面板，支持拖拽转移与同库存交换，预留过滤器接口。

---

## 5：架构与框架奠基（3月 – 4月初）

- **基础架构确立**：搭建 `GameCore`、`NetworkCore`（早期ENet P2P）、`UIPanelBase`，确立单例管理器、无全局静态、服务定位器模式。
- **实体继承体系**：建立 `CreatureBase → Humanoid → Player` 以及 `CreatureBase → Humanoid → Npc` 双线，统一移动、重力、动画树、视线等基础设施。
- **数据表现分离铁律**：移除 `PlayerData` 对场景节点的直接引用，确保存档安全、可网络同步；引入 `PlayerManager` 统一管理玩家实例。
- **资源管理器**：实现按脚本类型自动分类加载预制体，支持 `Player`、`ItemComp`、UI 面板等快速获取。
- **存档系统**：`SaveData` 采用 `Dictionary<int, PlayerData>` 结构，支持多角色选择、增删存档；`PlayerData` 继承 `Resource`，内置背包数据，全程深拷贝保护。
- **初始化流程**：解决 `AddChild(null)`、`private set` 编译错误等时序问题，确保所有管理器 `Init()` 显式调用，依赖顺序可控。

---