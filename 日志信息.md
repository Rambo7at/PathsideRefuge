## ---------- 2026.4.29 开发日志 ----------
### 架构优化 · 消除数据层对表现层引用，存档系统健壮性升级
- ✅ 移除 `PlayerData` 中对 `Player` 节点的直接引用（`m_LocalPlayer`），彻底解决存档序列化风险、内存泄漏隐患及跨场景引用崩溃问题
- ✅ 确立“数据表现分离”铁律：`PlayerData` 作为纯数据 `Resource`，不再持有任何场景节点，确保存档安全、可网络同步
- ✅ 新增 `PlayerManager.GetLocalPlayer()` 方法，通过 `ActivePlayers` 字典安全获取本地玩家节点，替代原先危险的数据内反向引用
- ✅ 统一全局访问路径：所有组件（如 `SlotComp`）通过 `PlayerManager.GetLocalPlayer()` 获取节点，消除空引用崩溃

### 存档选择系统 · ID驱动索引替代位置索引
- ✅ 重构 `SaveData` 选择逻辑：用 `PickPlayer`（玩家ID）替代 `index`（数组索引），支持存档增删后仍能准确定位当前选中角色
- ✅ 实现 `GetPickPlayerData()` 安全查询，自动处理空存档、ID失效回退，保证返回值永远有效
- ✅ 实现 `PickNextPlayer()` 切换逻辑，利用 Godot `Array.IndexOf` 实现高效循环选择，支持无缝切人
- ✅ 移除 UI 中直接修改全局 `index` 的脆弱属性，改为本地缓存+确认提交模式，防止误修改

### Bug修复与健壮性强化
- ✅ 修复 `SlotComp.DoDrag` 中因 `PlayerData` 持有无效节点引用导致的 `NullReferenceException`
- ✅ 为所有跨模块调用添加空引用防御，保证在资源未就绪时不会崩溃
- ✅ 梳理并统一数据流：`PlayerData` ↔ `PlayerManager` ↔ 表现层，实现单向依赖、易于测试

### 整体里程碑
- ✅ 数据层（`PlayerData`、`SaveData`）彻底解耦表现层，具备联网、多存档扩展能力
- ✅ 存档选择系统健壮，支持动态增删存档、多角色切换，逻辑清晰可维护
- ✅ 框架核心原则（服务定位器+数据分离）落地深入，所有改动均严格遵循架构规范

## ---------- 2026.4.14 开发日志 ----------
### 背包系统 · 存档读档闭环与持久化完整落地
- ✅ 彻底修复读档后物品无法交换问题，统一格子创建流程，强制唯一 SlotID 0~9，杜绝ID重复与引用共享
- ✅ 优化背包数据刷新逻辑，采用打开背包时手动调用 RefSlot()，解决 _Ready() 刷新时机过早、UI不显示问题
- ✅ 完善 InventoryComp 读档逻辑，先实例化独立格子，再回填存档物品数据，全程使用深拷贝保证数据安全
- ✅ 验证全流程稳定：物品添加、拖拽、交换、堆叠、保存、读档、重新打开全部正常无BUG
- ✅ 严格遵循项目架构：数据表现分离、单例管理器、无全局静态、格子自治、玩家绑定规范

### 框架稳定与细节优化
- ✅ 清理管理器构造函数业务逻辑，规避数组越界、资源加载空引用等启动崩溃问题
- ✅ 完善存档文件存在性判断，避免启动时 Missing File 报错
- ✅ 统一代码注释规范，保持与项目整体风格一致
- ✅ 背包系统完全独立、可复用、无耦合，符合框架设计标准

### 整体里程碑
- ✅ 背包系统功能 + 数据持久化 + 存档读档 完整闭环
- ✅ 成为项目首个全流程稳定、可上线使用的业务模块
 --------------------
## ---------- 2026.4.11 开发日志 ----------
### 背包系统 · 数据持久化与存档闭环完成
- ✅ 修复存档数据读取为空问题，添加 [Export] 与 [GlobalClass] 实现正确序列化
- ✅ 重构 InventoryComp，新增 BindPlayer 绑定玩家，实现数据一一对应
- ✅ 实现存档自动加载、SaveData 同步、RefSlot 自动保存，完成运行时→存档闭环
- ✅ 全面使用深拷贝，杜绝引用错乱，保证数据安全
- ✅ 验证全流程：添加、拖拽、交换、存档、读档完全正常

### 代码规范与结构优化
- ✅ 统一类结构排序与注释规范
- ✅ 清理冗余代码，优化变量与私有成员管理
- ✅ 严格遵循表现/数据分离架构

### 框架稳定与功能闭环
- ✅ 背包系统功能完全闭环，稳定可用
- ✅ 严格遵守项目单例、无静态、格子自治规范

## ---------- 历史内容（2026.3.6 - 2026.4.10） ----------
- 核心架构：搭建 GameCore、NetworkCore（ENet P2P 局域网联机）、UIPanelBase 基础框架，确立单例管理器、无全局静态规范。
- 实体系统：建立 CreatureBase→Humanoid→Player 继承体系，实现生命、受击、死亡及网络权威控制，完成 Player 与 PlayerController 职责分离。
- 主菜单与控制台：完成主菜单 UI 切换、存档检测、局域网联机功能；开发 ConsoleComp+ConsoleManager，实现基础命令解析与物品生成对接。
- 背包系统（前期）：完成 SlotComp、SlotData 开发，实现格子自治、无冗余拖拽、物品交换功能，统一命名规范，清理冗余代码，确立 Resource 存档基础规则。
- 物品与互动：完成物品系统、拾取功能，开发快递盒、BoxComp 存储箱基础组件；完善背包交换、拆分逻辑，搭建建造系统基础。