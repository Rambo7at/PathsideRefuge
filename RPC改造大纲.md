# RPC 跨场景同步改造 - 实施大纲

> **更新日期**：2026.08.08 | **当前阶段**：单机验证 ✅ → 多人改造 🚀


## 1. 总目标

- 场景切换后，RPC 同步仅在同一场景内生效
- 跨场景玩家互相屏蔽 RPC 调用，杜绝异常报错
- 单机阶段已全部完成，现已进入多人跨场景同步改造阶段


## 2. 已完成（单机阶段 ✅）

- 单机场景切换全链路（`WorldManager.ChangeScene` + `ScenePortalComp` + 相机归还）
- 玩家数据持久化（`LastSceneHash` / `LastPosition` / `LastRotation`）
- 管理器架构重构（`SaveManager` 降级 IO，`WorldManager` / `PlayerManager` 数据自治）
- 装备 / 背包 / 武器跨场景完整保留验证通过
- 双向场景切换（主场景 ↔ 测试房屋）稳定运行

> 详细成果见开发计划书阶段五~七


## 3. 当前主线：多人跨场景 RPC 同步改造

### 3.1 场景玩家列表维护
- `WorldManager` 维护 `Dictionary<int, List<int>> _scenePlayers`
- `SceneBase._EnterTree` 注册玩家进入，`_ExitTree` 反注册
- 客户端进入场景后向主机汇报

### 3.2 临时主机选举机制
- 无主机时，最先进入场景的玩家自动成为临时主机
- 临时主机负责托管场景内 `NetSyncBase` 数据
- 全局主机进入时，临时主机移交数据快照并降级为普通客户端

### 3.3 快照同步机制
- 临时主机维护场景内所有 `NetSyncBase` 对象的数据快照
- 新玩家进入场景时，临时主机下发快照
- 全局主机接管时，临时主机上报完整快照

### 3.4 RPC 场景隔离
- `NetSyncBase.Rpc_Anypeer` 增加场景哈希校验
- 跨场景 RPC 自动拦截，仅同场景内广播
- 场景主机转发本场景 RPC，隔离跨场景流量


## 4. 后续待办

| 优先级 | 任务 | 状态 |
|--------|------|------|
| P0 | `WorldManager._scenePlayers` 玩家列表维护 | ⬜ |
| P0 | `SceneBase` 接入玩家注册/反注册 | ⬜ |
| P0 | `NetSyncBase.Rpc_Anypeer` 场景哈希校验 | ⬜ |
| P1 | 临时主机选举逻辑 | ⬜ |
| P1 | 快照同步机制（临时主机 → 新客户端） | ⬜ |
| P2 | 传送过渡效果（淡入淡出 / 加载遮罩） | ⬜ |
| P2 | 地面物品场景切换保存（注册为 `NetObject`） | ⬜ |


## 5. 设计图（多人跨场景）
场景 A 场景 B
│ │
│── 玩家进入 ──→ 注册到 _scenePlayers
│ │
│ 无主机 → 第一个进入者成为临时主机
│ │
│ 临时主机托管 NetSyncBase 快照
│ │
│── 全局主机进入 ──→ 快照移交 → 降级为普通客户端
│ │
└── RPC 场景隔离 ──→ 仅同场景内广播 ──┘