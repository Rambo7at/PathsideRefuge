# RPC 跨场景同步改造 - 实施大纲（更新版）

> **更新日期**：2026.08.06 | **当前进度**：单机阶段核心功能已全部完成 ✅


## 1. 总目标

- 场景切换后，RPC 同步仅在同一场景内生效
- 跨场景玩家互相屏蔽 RPC 调用，杜绝异常报错
- 现阶段落地单机场景切换开发，预留拓展接口支撑后续多人跨场景开发


## 2. 实施优先级（已完成 ✅）

### 2.1 阶段 0 存储系统梳理 ✅
- 梳理 `InventoryData`、`Equipment` 现有序列化与反序列化逻辑
- 区分场景切换过程中需要快照留存、可直接丢弃的数据
- **完成情况**：跨场景传送后武器、装备、物品数据完整保留，序列化链路验证通过

### 2.2 阶段 1 单机场景切换核心实现 ✅
- 依托 `WorldManager` 搭建场景加载、卸载基础逻辑
- 仅做单机逻辑开发，暂不接入网络模块
- 校验场景切换后玩家实例复用/重建逻辑稳定性
- **完成情况**：
  - `WorldManager.ChangeScene` 核心方法开发完成
  - 玩家重新实例化方案落地（`PlayerManager.SpawnLocalPlayer` 自动检测无效引用并重建）
  - `PlayerCamera` 相机归还机制完成（`_ExitTree` 归还 `gameCamera` 给 `WorldManager`）
  - 物理回调冲突已解决（`ScenePortalComp` 使用 `CallDeferred` 延迟切换）

### 2.3 阶段 2 测试场景搭建：房屋场景 ✅
- 新建独立场景 `测试场景_b.tscn`（房屋）
- 作为固定测试载体，用于场景进出切换测试
- **完成情况**：测试场景已搭建，`SceneBase` 继承正确，含 `SpawnPoint` 出生点

### 2.4 阶段 3 场景切换触发器（传送门）开发 ✅
- 部署在房屋入口位置
- 物理载体：`Area3D`
- 触发方式：`BodyEntered` 信号检测玩家进入
- 作用：控制玩家在不同场景间切换
- 命名：`ScenePortalComp`
- **完成情况**：
  - `ScenePortalComp` 组件开发完成
  - 支持 `Export` 配置目标场景名称
  - 物理回调安全处理（`CallDeferred` 延迟执行）
  - 防重复触发机制（`isTriggering` 标志）

### 2.5 阶段 4 单机双向切换全流程验证 ✅
- 主场景与房屋场景来回切换测试
- 校验玩家坐标、相机、UI 重建状态
- 校验装备数据切换前后一致性
- **完成情况**：
  - 主场景 → 房屋场景 切换 ✅
  - 房屋场景 → 主场景 切换 ✅
  - 玩家手持武器状态保持 ✅
  - 相机正确激活 ✅
  - 场景内地面物品不保存（已知限制，不影响核心功能）


## 3. 已解决的关键问题记录

| 问题 | 解决方案 | 状态 |
|------|---------|------|
| 物理回调中销毁节点报错 | `ScenePortalComp` 使用 `CallDeferred` 延迟执行 `ChangeScene` | ✅ |
| 场景切换后玩家被销毁 | `SpawnLocalPlayer` 检测 `IsInstanceValid`，无效则重新实例化 | ✅ |
| 切换后相机丢失导致黑屏 | `PlayerCamera._ExitTree` 归还 `gameCamera` 给 `WorldManager` | ✅ |
| 玩家数据在切换后丢失 | 通过 `m_LocalPlayerData` 保留完整数据，重新实例化后重新赋值 | ✅ |
| 重复添加玩家报错 | `SpawnLocalPlayer` 添加前检查父节点并移除 | ✅ |
| 场景切换后地面物品不保存 | 已知限制，场景卸载时非网络对象被销毁；正常 ESC 保存可恢复 | ⚠️ 非阻塞 |


## 4. 后续多人扩展规划（当前阶段搁置）

- 划分单场景托管 `NetSyncBase` 对象集合
- 制定场景主机选举规则，无主机时优先入场玩家自动成为主机
- 新主机接入后，下发全量数据快照同步
- RPC 调用前置增加同场景校验逻辑
- **单机阶段已有的跨场景物品保存能力**可作为多人跨场景同步的基础参考


## 5. 单机阶段完成检查清单

- [x] 梳理 `InventoryData`、`Equipment` 序列化机制
- [x] 确定玩家实例销毁重建方案（重新实例化 + `m_LocalPlayerData` 数据保留）
- [x] 制定场景切换时背包、装备数据留存方案（`m_LocalPlayerData` 跨场景传递）
- [x] 完成房屋场景基础搭建（墙体、地板、传送门点位）
- [x] 编写 `ScenePortalComp` 交互逻辑（`Area3D` 检测 + `CallDeferred` 延迟切换）
- [x] 完成主场景、房屋场景双向切换测试
- [x] 校验场景切换前后装备数据完整性（武器保留验证通过）


## 6. 后续待办（按优先级）

| 优先级 | 任务 | 状态 |
|--------|------|------|
| P1 | 复习存储系统完整链路（`SaveManager` + `WorldData` + `SceneData` 联动） | ⬜ |
| P2 | 多场景持续切换压力测试（10 次以上来回切换） | ⬜ |
| P2 | `ScenePortalComp` 增加传送过渡效果（淡入淡出 / 加载遮罩） | ⬜ |
| P3 | 跨场景 RPC 同步框架设计（预留接口） | ⬜ |
| P3 | 多人模式场景切换支持（主机权威广播） | ⬜ |


## 7. 单机阶段涉及代码文件（已修改/新增）

| 文件 | 改动 | 状态 |
|------|------|------|
| `WorldManager.cs` | `ChangeScene` 方法完善，玩家迁移逻辑（重新实例化方案） | ✅ |
| `SceneBase.cs` | `_EnterTree` 恢复网络对象（跳过玩家） | ✅ |
| `Player.cs` | 添加 `_Ready` 日志诊断，`_ExitTree` 清理预留 | ✅ |
| `PlayerManager.cs` | `SpawnLocalPlayer` 增加引用有效性检测和重新实例化逻辑 | ✅ |
| `PlayerCamera.cs` | 新增 `_ExitTree` 相机归还逻辑 | ✅ |
| `ScenePortalComp.cs` | 新增组件，`Area3D` 检测 + `CallDeferred` 延迟切换 | ✅ |
| `测试场景_b.tscn` | 新增房屋测试场景 | ✅ |


## 8. 风险提示（已处理）

1. ~~场景卸载时清理残留 CanvasLayer，防止 UI 冗余错乱~~ → 玩家重新实例化时 UI 由 `Player._Ready` 重建，无残留
2. ~~玩家实例销毁方案：切换前置完成背包、装备数据持久化保存~~ → 通过 `m_LocalPlayerData` 保留，切换后重新赋值
3. ~~物理回调冲突~~ → `CallDeferred` 延迟执行已解决


## 9. 备注

- 地面物品不保存是单机场景切换的正常行为（非 `NetObject` 的对象不进入存档）。如需保存，需将物品注册为 `NetObject` 并通过 `NetObjectRegistry` 管理。
- 当前玩家采用**重新实例化方案**，状态通过 `m_LocalPlayerData` 传递，不影响游戏体验。
- 后续多人模式如需保持玩家对象连续，可考虑**迁移方案**（`RemoveChild` + `AddChild`），单机阶段暂不要求。